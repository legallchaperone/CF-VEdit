# scripts — entrypoints

Distributed training, data preparation, and benchmark-run launchers. Thin
wrappers over `integration/` and the packages; no logic of their own.

Examples (to be added): `train.sh <config> <ngpu>` (≥8×A100 per sa2va-plan §1.D),
`prepare_sim_data.py`, `run_on_benchmark.sh <run_name>`.
