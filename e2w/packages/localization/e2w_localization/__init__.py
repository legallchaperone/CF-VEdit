"""e2w_localization — 定位半: Causal Planner + Mask decoder (Sa2VA-based).

Produces the seam types from ``e2w_core`` (``ThreeLayerMask``, ``EditPlan``).
Skeleton only — see the package README and sa2va-plan changes A–D. Depends on
``e2w_core`` only; must never import ``e2w_generation`` (boundary B3).
"""
